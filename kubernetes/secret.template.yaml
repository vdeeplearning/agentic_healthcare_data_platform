# Template only. Copy outside source control, replace every placeholder, and apply securely.
apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: agentic-healthcare
type: Opaque
stringData:
  POSTGRES_DB: clinical
  POSTGRES_USER: REPLACE_ME
  POSTGRES_PASSWORD: REPLACE_ME
  POSTGRES_DSN: postgresql://REPLACE_ME:REPLACE_ME@postgres:5432/clinical
  DATABASE_URL: postgresql://REPLACE_ME:REPLACE_ME@postgres:5432/clinical
  AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://REPLACE_ME:REPLACE_ME@postgres:5432/airflow
  AIRFLOW__CORE__FERNET_KEY: REPLACE_WITH_GENERATED_FERNET_KEY
  AIRFLOW__WEBSERVER__SECRET_KEY: REPLACE_WITH_RANDOM_SECRET
  CLINICAL_SQL_OPENAI_API_KEY: REPLACE_OR_REMOVE_IF_UNUSED

